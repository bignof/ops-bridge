# 这些文件随镜像在 Linux 容器内执行/解析,强制 LF,避免 Windows 检出成 CRLF
# 导致 shebang(#!/bin/sh\r)失效或 nginx/supervisor 解析异常。
*.sh           text eol=lf
nginx.conf     text eol=lf
supervisord.conf text eol=lf
Dockerfile     text eol=lf
